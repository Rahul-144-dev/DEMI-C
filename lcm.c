/*Find LCM of two numbers*/
#include <stdio.h>
#include <math.h>
int main()
{
    int n1, n2, lcm, max;
    printf("enter number : ");
    scanf("%d", &n1);
    printf("enter number : ");
    scanf("%d", &n2);
    while (1)
    {
    }

    return 0;
}